<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.common.mapper.ChatMessageMapper">

    <!-- 메시지 저장 -->
    <insert id="insertChatMessage" parameterType="com.example.backend.common.model.ChatMessage">
        INSERT INTO chat_messages (chatRoomId, senderId, message, timestamp)
        VALUES (#{chatRoomId}, #{senderId}, #{message}, #{timestamp})
    </insert>

    <!-- 특정 채팅방의 메시지 목록 조회 -->
    <select id="selectMessagesByRoomId" resultType="com.example.backend.common.model.ChatMessage">
        SELECT id, chatRoomId, senderId, message, timestamp
        FROM chat_messages
        WHERE chatRoomId = #{chatRoomId}
        ORDER BY timestamp ASC
    </select>
</mapper>
