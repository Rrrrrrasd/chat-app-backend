<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.common.mapper.UserMapper">

    <resultMap id="UserModelResultMap" type="com.example.backend.common.model.UserModel">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>


    <select id="selectUserByUsername" parameterType="string" resultMap="UserModelResultMap">
        SELECT * FROM USERS
        WHERE username = #{username}
          AND deleted_at IS NULL
    </select>

    <select id="selectUserByNickname" resultMap="UserModelResultMap">
        SELECT * FROM USERS WHERE nickname = #{nickname}
    </select>

    <select id="existsByNickname" parameterType="String" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM USERS
        WHERE nickname = #{nickname}
          AND deleted_at IS NULL
    </select>

    <select id="selectUserById" parameterType="long" resultMap="UserModelResultMap">
        SELECT * FROM USERS
        WHERE id = #{id}
          AND deleted_at IS NULL
    </select>

    <insert id="insertUser" parameterType="com.example.backend.common.model.UserModel" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO USERS (username, password, nickname, created_at, updated_at)
        VALUES (#{username}, #{password}, #{nickname}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateUser" parameterType="com.example.backend.common.model.UserModel">
        UPDATE USERS
        SET username = #{username},
            password = #{password},
            nickname = #{nickname},
            updated_at = #{updatedAt},
            deleted_at = #{deletedAt}
        WHERE id = #{id}
    </update>

</mapper>