<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend.common.mapper.MessageReadMapper">
    <insert id="insertMessageRead" parameterType="com.example.backend.common.model.MessageReadModel">
        INSERT INTO MESSAGE_READS(message_id, user_id, read_at)
        VALUES(#{messageId}, #{userId}, #{readAt})
    </insert>

    <select id="selectMessageRead" resultType="com.example.backend.common.model.MessageReadModel">
        SELECT * FROM MESSAGE_READS WHERE message_id = #{messageId} AND user_id = #{userId}
    </select>

    <update id="updateMessageRead" parameterType="com.example.backend.common.model.MessageReadModel">
        UPDATE MESSAGE_READS
        SET read_at = #{readAt}
        WHERE message_id = #{messageId} AND user_id = #{userId}
    </update>
</mapper>

